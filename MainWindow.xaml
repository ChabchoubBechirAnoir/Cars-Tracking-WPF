<Window x:Class="WpfMap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMap"
        mc:Ignorable="d"
        xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        Title="GetMeHome" Height="auto" Width="auto">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Margin="0,0,0,0">
            <TextBox x:Name="txtLocation" Height="20" Width="auto"/>
            <Button x:Name="btnAdd" Click="ButtonAddName_Click" Height="20" Width="50" Content="Search"/>
        </StackPanel>

        <gmaps:GMapControl x:Name="mapView" 
                           Loaded="mapView_Loaded"
                           MouseDoubleClick="MapWithPushpins_MouseDoubleClick"
                           Margin="0,40,0,0" 
                           Grid.Row="1" />

    </Grid>


</Window>
