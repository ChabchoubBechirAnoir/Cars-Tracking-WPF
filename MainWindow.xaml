<Window x:Class="WpfMap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMap"
        mc:Ignorable="d"
        xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        Title="GetMeHome" Height="auto" Width="auto">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <StackPanel VerticalAlignment="Bottom">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="txtLocation" Height="auto" Width="auto"/>
                <Button x:Name="btnGoToLocation" Click="GoToLocation" Height="auto" Width="auto" Content="Search"/>
            </StackPanel>
        </StackPanel>
        <gmaps:GMapControl x:Name="mapView" 
                           Loaded="mapView_Loaded"
                           MouseDoubleClick="MapWithPushpins_MouseDoubleClick"
                           Margin="0,20,0,0" 
                           Grid.Row="1" />

    </Grid>


</Window>
